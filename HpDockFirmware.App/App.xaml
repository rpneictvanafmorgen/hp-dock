<Application x:Class="HpDockFirmware.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF3EEE7" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFCF8F4" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF1F5F5B" />
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#FFC96F3A" />
        <SolidColorBrush x:Key="MutedBrush" Color="#FF6E675F" />
        <SolidColorBrush x:Key="BorderBrush" Color="#FFE5D9CB" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF1E1E1E" />
        </Style>

        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Application.Resources>
</Application>
